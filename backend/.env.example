# Server
PORT=3001

# MongoDB - local connection string for development
MONGODB_URI=mongodb://localhost:27017/thameltoronto

# JWT - Generate a random string for production (e.g. openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Nodemailer - For sending verification emails (Gmail example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
MAIL_FROM="Thamel Bar <your-email@gmail.com>"

# Verification code expiry (minutes)
VERIFICATION_CODE_EXPIRY=10

# Optional: Admin API secret. If set, admin routes require X-Admin-Secret header (used by admin Next app).
# ADMIN_SECRET=your-admin-secret
